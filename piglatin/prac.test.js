
const {
	pigLatinWords
} = require("./prac")

describe('pigLatinWords'), () => {
	it("adding 'way' and the first consonent to the end of the word"), () => {
		const vowels = "aeiou"
		let index = 0
		const result = ""

		pigLatinWords(word)
		expect(word).toEqual(result)
	}
}